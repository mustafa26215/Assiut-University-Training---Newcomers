#include <iostream>
using namespace std;
#include <cmath>
int main() {
    long long a,b;
    cin >> a>>b;
    int temp=min(a,b);
    int max=0;
    for(int i=1;i<=temp;i++){
        if((a%i==0)&&(b%i==0)){
            max=i;
        }
    }
    cout<<max;
    return 0;
}