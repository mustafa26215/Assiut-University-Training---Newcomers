#include <iostream>
using namespace std;

int main() {
    long long n, m, k;
    cin >> n >> m >> k;

    long long ans = 0;

    long long x = min({n, m, k});
    ans += x;

    n -= x;
    m -= x;
    k -= x;

    ans += min(n / 2, k);

    cout << ans;

    return 0;
}