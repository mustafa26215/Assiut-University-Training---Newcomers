#include <iostream>
#include <algorithm>
using namespace std;
 
int main() {
    int A, B, C;
    cin >> A >> B >> C;
 
    // store original values
    int a = A, b = B, c = C;
 
    // sort values
    int arr[3] = {A, B, C};
    sort(arr, arr + 3);
 
    // print sorted
    for (int i = 0; i < 3; i++)
        cout << arr[i] << endl;
 
    cout << endl;  // blank line
 
    // print original order
    cout << a << endl << b << endl << c;
 
    return 0;
}